<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </p>
  <%if user_signed_in?%>
 <%= f.hidden_field :user_id, :value => current_user.id%>
 <%else%>
 <%end%>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>